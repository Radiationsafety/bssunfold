# .github/workflows/deploy-marimo.yml
# This workflow builds and deploys the marimo notebook example to GitHub Pages.
# It runs on pushes to main or manual trigger.

# name: Deploy Marimo to GitHub Pages

# on:
#   push:
#     branches: ['main']
#   workflow_dispatch:

# concurrency:
#   group: 'pages'
#   cancel-in-progress: false

# env:
#   UV_SYSTEM_PYTHON: 1

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
      
#       - name: Install uv
#         uses: astral-sh/setup-uv@v6
      
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'
      
#       - name: Install marimo
#         run: uv sync --dev
      
#       - name: Create output directory
#         run: mkdir -p _site
      
#       - name: Export marimo notebook
#         run: |
#           marimo export html-wasm ./examples/02-marimo-notebook.py \
#             -o _site/index.html \
#             --mode run \
#             --no-show-code
      
#       - name: Create .nojekyll
#         run: touch _site/.nojekyll
      
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: _site

#   deploy:
#     needs: build
#     permissions:
#       pages: write
#       id-token: write
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4